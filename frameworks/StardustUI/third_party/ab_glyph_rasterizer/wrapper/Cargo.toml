[package]
name = "stardustui_ab_glyph_rasterizer_capi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "cdylib"]
path = "src/lib.rs"

[dependencies]
ab_glyph_rasterizer = { path = "../upstream/rasterizer", default-features = true }
